using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIJobManager : MonoBehaviour
{
    // Start is called before the first frame update
    private void OnMouseDown()
    {
        // Votre code à exécuter lors du clic sur la boîte
        Debug.Log("La souris a cliqué sur la boîte !");
    }
}
